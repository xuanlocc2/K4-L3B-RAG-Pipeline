# Threshold Calibration

> Auto-generated by `scripts/calibrate_threshold.py` on 2026-09-25.

## Method

We measured the **original dense cosine score** (not the RRF score) returned by
`semantic_search` for two groups of queries:

* **In-domain (10 queries):** câu hỏi về quy chế tuyển sinh, tổ hợp môn, điều
  kiện xét tuyển, các phương thức, hồ sơ, kỳ thi tốt nghiệp, chứng chỉ IELTS.
* **Out-of-domain (5 queries):** công thức nấu ăn, giá vàng, thủ tục đăng ký
  kết hôn, lịch thi đấu World Cup, cách trồng cây cảnh.

For each query we recorded the top-1 dense cosine similarity (1 - cosine
distance). The threshold was selected from a set of candidates and chosen to
**maximise in-domain coverage** while **minimising out-of-domain leakage**.

## Score distribution

| Group        | Count | Min   | Max   | Mean  |
| ------------ | ----: | ----: | ----: | ----: |
| In-domain    |    10 | 0.565 | 0.703 | 0.644 |
| Out-of-domain|     5 | 0.368 | 0.494 | 0.436 |

The two distributions are well separated: in-domain min (0.565) > out-of-domain
max (0.494), giving a clear gap of 0.07.

## Candidates evaluated

| Threshold | In-domain misses | OOD false-keep | Notes                         |
| --------: | ----- | -------------- | ----------------------------- |
| 0.20      | 0     | 5              | Quá thấp, không fallback OOD. |
| 0.25      | 0     | 5              | Quá thấp.                     |
| 0.30      | 0     | 5              | Quá thấp.                     |
| 0.35      | 0     | 5              | Quá thấp.                     |
| 0.40      | 0     | 4              | Chưa cắt hết OOD.             |
| 0.45      | 0     | 2              | Gần tối ưu.                   |
| **0.50**  | **0** | **0**          | **Đã chọn** — cắt sạch OOD, không mất in-domain. |
| 0.55      | 1     | 0              | Bắt đầu hy sinh in-domain.    |

## Decision

* **Selected threshold = 0.50.**
* Đặt trong `src/task9_retrieval_pipeline.py` (override được qua env
  `SCORE_THRESHOLD`).
* Khi `best_dense_score < 0.50`, pipeline thử PageIndex fallback. Vì
  PageIndex không có key trong môi trường mặc định, fallback trả về `[]`
  và pipeline trả về hybrid (đã an toàn).
* Khi không có fallback trả về kết quả, pipeline vẫn trả hybrid — giúp hệ
  thống không bao giờ crash vì OOD.

## Limitations

* Calibration dùng 10 in-domain + 5 OOD queries. Với corpus lớn hơn nên tăng
  kích thước tập validation.
* Threshold chỉ phù hợp với embedding `BAAI/bge-m3`; đổi model có thể thay
  đổi phân phối score và cần hiệu chỉnh lại.
* Threshold không đo thời gian truy vấn; với retrieval vector DB lớn, cân
  nhắc cache để giảm latency.